/**
 * Splits a story text into a 30% free preview and 70% locked content.
 */
export declare const splitStoryPreview: (text: string) => {
    preview: string;
    locked: string;
    previewPercentage: number;
};
//# sourceMappingURL=storyUtils.d.ts.map